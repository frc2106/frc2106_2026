// Copyright (c) 2024 - 2025 : FRC 2106 : The Junkyard Dogs
// https://www.team2106.org

// Use of this source code is governed by an MIT-style
// license that can be found in the LICENSE file at
// the root directory of this project.

package frc.robot.subsystems.superstructure;

/*
public class RobotStatus {

	enum RobotState {
		IDLE,
		INTAKE,
		EJECT


	// Robot state
	public RobotState currentRobotState = RobotState.IDLE;

	// Subsystem statuses (created once, reused)
	public final AngleStatus angle = new AngleStatus();
	public final IntakeStatus intake = new IntakeStatus();
	public final IntakeSliderStatus intakeSlider = new IntakeSliderStatus();
	public final ShooterStatus shooter = new ShooterStatus();

	public RobotStatus() {} // Objects already initialized above

	// Update method to modify in-place instead of creating new objects
	public void update(
			RobotState currentRobotState,
			Rotation2d targetRobotAngle,
			Rotation2d currentRobotAngle,
			boolean isAtTargetRobotAngle,
			double targetIntakeWheelVoltage,
			double currentIntakeWheelVoltage,
			double targetIntakeSliderPositionMeters,
			double currentIntakeSliderPositionMeters,
			boolean isAtTargetIntakeSliderPositionMeters,
			double targetShooterSpeedRPM,
			double currentShooterSpeedRPM,
			boolean isAtTargetShooterSpeedRPM) {
		this.currentRobotState = currentRobotState;

		this.angle.target = targetRobotAngle;
		this.angle.current = currentRobotAngle;
		this.angle.isAtTarget = isAtTargetRobotAngle;

		this.intake.targetWheelVoltage = targetIntakeWheelVoltage;
		this.intake.currentWheelVoltage = currentIntakeWheelVoltage;

		this.intakeSlider.targetPositionMeters = targetIntakeSliderPositionMeters;
		this.intakeSlider.currentPositionMeters = currentIntakeSliderPositionMeters;
		this.intakeSlider.isAtTarget = isAtTargetIntakeSliderPositionMeters;

		this.shooter.targetRPM = targetShooterSpeedRPM;
		this.shooter.currentRPM = currentShooterSpeedRPM;
		this.shooter.isAtTarget = isAtTargetShooterSpeedRPM;
	}
}
*/
